import { ExtensionDatePipe } from './extension-date.pipe';

describe('ExtensionDatePipe', () => {
  it('create an instance', () => {
    const pipe = new ExtensionDatePipe();
    expect(pipe).toBeTruthy();
  });
});
